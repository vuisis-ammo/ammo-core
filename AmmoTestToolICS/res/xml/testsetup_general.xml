<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android" >

    

    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->
    <EditTextPreference
        android:capitalize="words"
        android:defaultValue="Ammo Test #1"
        android:inputType="textCapWords"
        android:key="test_name_memo"
        android:maxLines="1"
        android:selectAllOnFocus="true"
        android:singleLine="true"
        android:title="Test Name (optional)" />

    <!--
         NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it.
    -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference android:defaultValue="1" android:entries="@array/pref_example_list_titles" android:entryValues="@array/pref_example_list_values" android:key="contract_list" android:negativeButtonText="@null" android:positiveButtonText="@null" android:title="Contract" android:enabled="true" /><EditTextPreference android:key="test_duration" android:title="Test Duration (min)" android:defaultValue="1"/><EditTextPreference android:key="msg_rate" android:title="Message Delay (msec)" android:defaultValue="1000"/>
    <CheckBoxPreference android:summary="Random if checked, constant otherwise" android:title="Random message content" android:key="random_msg_content" android:defaultValue="true"/>

</PreferenceScreen>